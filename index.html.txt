<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Image Click Timer</title>
  <style>
    /* Prevent scrolling and center content */
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      width: 100%;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #f0f0f0;
    }
    #image-container {
      position: relative;
      display: inline-block;
      /* Constrain container to viewport */
      max-width: 100vw;
      max-height: 100vh;
    }
    #main-image {
      display: block;
      /* Scale down large images to fit viewport */
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
    }
    /* Click zone visible as solid black */
    #click-zone {
      position: absolute;
      top: 20%;    /* adjust as needed */
      left: 30%;   /* adjust as needed */
      width: 200px; /* adjust as needed */
      height: 150px;/* adjust as needed */
      cursor: pointer;
      background: black;
    }
    /* Simple modal popup */
    #timer-modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      display: none;
      align-items: center;
      justify-content: center;
    }
    #modal-content {
      background: #fff;
      padding: 20px;
      border-radius: 8px;
      text-align: center;
      min-width: 200px;
      font-family: sans-serif;
    }
    #close-btn {
      margin-top: 10px;
      padding: 5px 10px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div id="image-container">
    <img id="main-image" src="your-image.jpg" alt="Click Area Image" />
    <div id="click-zone"></div>
  </div>

  <!-- Modal -->
  <div id="timer-modal">
    <div id="modal-content">
      <p id="time-display"></p>
      <button id="close-btn">Close</button>
    </div>
  </div>

  <script>
    let startTime;
    let elapsed;
    let timerRunning = false;

    window.addEventListener('load', () => {
      // Start the timer when page loads
      startTime = Date.now();
      timerRunning = true;
    });

    const clickZone = document.getElementById('click-zone');
    const timerModal = document.getElementById('timer-modal');
    const timeDisplay = document.getElementById('time-display');
    const closeBtn = document.getElementById('close-btn');

    clickZone.addEventListener('click', () => {
      if (!timerRunning) return;
      elapsed = Date.now() - startTime;
      timerRunning = false;
      showModal(elapsed);
    });

    closeBtn.addEventListener('click', () => {
      timerModal.style.display = 'none';
    });

    function showModal(ms) {
      // Convert ms to seconds with milliseconds
      const seconds = (ms / 1000).toFixed(3);
      timeDisplay.textContent = `Your time: ${seconds} seconds`;
      timerModal.style.display = 'flex';
    }
  </script>
</body>
</html>
